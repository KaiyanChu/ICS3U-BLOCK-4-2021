package week2;


public class hw1 {
    public static void main(String[] args) {
       /**  
        //hw q1. 
        double num = 72;
        double numSqrd = num * num;
        System.out.println(num + " x " + num + " = " + numSqrd); 
        
        //hw q2
        int numQuestions = 11;
        int scoreRecieved = 9;

        double percentage = ((double)scoreRecieved/numQuestions)*100;

        System.out.println("the student's percentage was " + percentage + " %");

        //hw q3
        int num = 57392;
        int num2 = (num/1000)%10;
        int num4 = (num/10)%10;
        System.out.println(num2*num4);
        
        //hw q4
        int iVel = 83;  // m/s
        int accel = 36; // m/s*2
        int time = 2;   //s
        int fVel = (iVel + accel*time);
        System.out.println("the final velocity is " + fVel);
        

        //hw q5
        int radius = 6;
        double area = (Math.PI*(radius*radius));
        System.out.println("The area is " + area);
        

        //hw q6
        int radius = 6;
        double volume =((4/3)*Math.PI*(radius*radius*radius));
        System.out.println("the volume is " + volume);


        
        int a = 1;
        int b = 100;
        int c = 2;
        int x = 8;
        int y = (a*(x*x) + b*x + c);
        System.out.println("y = " + y);
        
        //hw q10
        double root1 = -b + (Math.sqrt(b*b-(4*a*c)))/(2*a);
        double root2 = -b - (Math.sqrt(b*b-(4*a*c)))/(2*a);
        System.out.println("the roots are " + root1 + " and " + root2);

        
        //hw q8
        int x1 = 5;
        int x2 = 6;
        int y1 = 2;
        int y2 = 4;
        double slope = ((double)(y2-y1)/(x2-x1));
        System.out.println("the slope is " + slope);
        
        
        //hw q9
        int pennies = 3;
        int nickels = 4;
        int dimes = 5;
        int quarters = 6;
        int loonies = 7;
        int toonies = 8;
        double totalMoney = (0.01*pennies) + (0.05*nickels) + (0.1*dimes) + (0.25*quarters) + (loonies) + (2*toonies);
        System.out.println("the total money is " + totalMoney);
        */

        //hw q11
        double x = 5.5, y = 3.0, z = -2.0;
        int i = 5, j = 4, k = 3;


        //hw 12
        
    
        



    }
}
